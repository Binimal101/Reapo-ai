/**
 * Optional bridge for Langfuse realtime / socket features.
 * Reserved for future use; dashboard currently uses HTTP trace snapshots.
 */
export function createLangfuseSocketBridge() {
  return null;
}
